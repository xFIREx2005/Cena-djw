const CORES = [
			'#3CB371',
			'#006400',
			'#008000'
		];
	
		

Object.freeze(CORES);

var cor = CORES[Math.floor(Math.random()*CORES.length)];


const FUNDO = [
			
			'#2E8B57',
			'#006400',
			'#008000'
		];
		

Object.freeze(FUNDO);

var fundo = FUNDO[Math.floor(Math.random()*FUNDO.length)];

const KEY = {
  C: 67,
  X: 88,
  Z: 90,
  UP: 38,
  DOWN: 40
}